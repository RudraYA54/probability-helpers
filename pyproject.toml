[project]
name = "probability-helpers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.0",
]
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]